import Phaser from 'phaser';

// Mapeo de clases creadas por Tiled (las subclases pueden extender este mapa)
export const CLASS_MAP = {};

export default class BaseLevelScene extends Phaser.Scene {

    constructor(key) {
        super({ key });
        this.score = 0;
        this.coinScore = 0;
        this.purpleCoinScore = 0;
        this.enPausa = false;
        this.endTimer = false;
        this.powerups = null;
        this.hammers = null;
        this.openedScene = false;
    }

    // --------------------------------------------------------------------------------------
    // PRELOAD GENÉRICO
    // --------------------------------------------------------------------------------------
    preload() {
        console.log(`=== PRELOAD ${this.scene.key} ===`);
    }

    // --------------------------------------------------------------------------------------
    // MÉTODO CREATE PRINCIPAL
    // (Las escenas hijas pueden extenderlo llamando a super.create())
    // --------------------------------------------------------------------------------------
    create() {
    }
}